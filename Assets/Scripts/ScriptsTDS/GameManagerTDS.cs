using UnityEngine;

public class GameManagerTDS : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Cursor.visible = false;
        Cursor.lockState = CursorLockMode.Confined; // Para que no se vaya del borde de la pantalla
    }

    
}
